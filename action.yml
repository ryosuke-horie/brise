name: "brise"
description: "Issue-[Issue Number] Branch Creator"
author: "ryosuke-horie"

runs:
  using: "composite"
  steps:
    - uses: actions/setup-go@v2
      with:
        go-version: 1.23
    - name: run main.go
      shell: bash
      run: go run main.go
